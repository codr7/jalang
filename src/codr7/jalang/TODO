* Add head/tail(Pair):Any functions
** Add Any type

* add split function
** (split "foo bar baz" " ")

String[] parts = string.split(Pattern.quote("."));

* Add let macro
** Allocate registers
*** Bind in namespace
** Test increment/decrement

* add =/</> functions
** add equalValues Type method
*** use to implement Value.equals

* Add user defined functions

(function [x:Int]:Int x)
