* add string function
** use say to convert arguments

* add String/reverse function

* add Int/parse function

* Add let macro
** Allocate registers
*** Bind in namespace
** Test increment/decrement

(let [x 1]
  (+1 x))

* add =/</> functions
* Add user defined functions

(func [x:Int]:Int x)

* Check argument types