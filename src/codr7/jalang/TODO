* convert map output to custom iterator
** add MapIterator type

* add reduce function
** call iterator on argument

* add user defined functions

(func [x:Int]:Int x)

* figure out how to map/reduce user defined functions

* aoc/code1.jl

* add let macro
** allocate registers
*** bind in namespace
** test increment/decrement

(let [x 1]
  (+1 x))

* add =/</> functions

* check function argument types

* turn pair into collection/sequence type
